/**
 * Placeholder — Overlay that highlights scheduling conflicts on the timeline.
 * Will render red-highlighted regions when tasks overlap with conflicting parties
 * or when a task is scheduled during a blackout/holiday period. Phase 3.
 */
export function ConflictOverlay() {
  return null;
}
